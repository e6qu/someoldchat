# Contract sources

This directory contains immutable upstream contract copies, explicit local
overlays, generated catalogs, and provenance evidence. Upstream material must
be added only with its exact revision, checksum, license, and retrieval record.

The machine-readable compatibility ledger lives at
[`specs/compatibility.yaml`](../specs/compatibility.yaml).
